function SectionTitle({ title }) {
  return (
    <h2
      style={{
        fontSize: "32px",
        marginBottom: "20px",
        marginTop: "40px",
      }}
    >
      {title}
    </h2>
  );
}

export default SectionTitle;